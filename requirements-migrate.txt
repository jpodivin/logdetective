# Keep these pins aligned with poetry.lock so migrations use the application's
# exact database and queue library versions.
alembic==1.18.4
pgvector==0.4.2
procrastinate==3.9.0
psycopg[binary,pool]==3.3.5
psycopg-pool==3.3.1
sqlalchemy==2.0.49
tenacity==9.1.4
